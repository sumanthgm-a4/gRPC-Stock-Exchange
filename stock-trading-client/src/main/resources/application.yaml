spring:
    application:
        name: stock-trading-client
    grpc:
        client:
            channels:
                stock-server:
                    address: "static://localhost:9090"
                    negotiation-type: PLAINTEXT

grpc:
    server:
        port: ${SERVER_PORT}